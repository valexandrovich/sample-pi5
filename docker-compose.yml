services:
  backend:
    build: ./backend
    environment:
      - PORT=3000
    expose:
      - "3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_BASE_PATH=/sample/
        - VITE_API_URL=/sample/api
    ports:
      - "${APP_PORT:-3115}:80"
    depends_on:
      - backend
    restart: unless-stopped

